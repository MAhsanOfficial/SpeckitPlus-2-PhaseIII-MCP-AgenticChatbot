# Environment Variables Template for Phase IV Deployment
#
# IMPORTANT: This file contains placeholder values only.
# Copy this file to backend/.env and fill with actual credentials before deployment.
# NEVER commit the actual .env file to version control.

# ============================================================================
# Database Configuration
# ============================================================================
# PostgreSQL connection string for Neon or other PostgreSQL database
# Format: postgresql://username:password@host:port/database?sslmode=require
# Required: Yes (Backend)
# Example: postgresql://neondb_owner:npg_xxx@ep-xxx.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# ============================================================================
# JWT Configuration
# ============================================================================
# Secret key for signing JWT tokens (minimum 32 characters recommended)
# Required: Yes (Backend)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-key-here-minimum-32-characters

# JWT signing algorithm (HS256 is standard for symmetric keys)
# Required: Yes (Backend)
# Options: HS256, HS384, HS512
JWT_ALGORITHM=HS256

# ============================================================================
# AI Configuration
# ============================================================================
# Google Gemini API key for AI chatbot functionality
# Required: Yes (Backend)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================================
# Frontend Configuration
# ============================================================================
# Backend API URL for frontend to connect to
# Required: Yes (Frontend)
#
# Local Development: http://localhost:8000
# Kubernetes Deployment: http://habit-tracker-backend:8000 (service DNS)
#
# NOTE: In Kubernetes, this value is set via ConfigMap, not .env file
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# Kubernetes Deployment Notes
# ============================================================================
#
# When deploying to Kubernetes:
# 1. Copy this file to backend/.env
# 2. Fill in actual values (DATABASE_URL, JWT_SECRET, GEMINI_API_KEY)
# 3. Create Kubernetes Secret:
#    kubectl create secret generic habit-tracker-secrets --from-env-file=backend/.env
# 4. NEXT_PUBLIC_API_URL is managed via ConfigMap (not Secret)
# 5. Deploy with Helm:
#    helm install habit-tracker ./helm/habit-tracker -f ./helm/habit-tracker/values-local.yaml
#
# For detailed deployment instructions, see DEPLOYMENT.md
