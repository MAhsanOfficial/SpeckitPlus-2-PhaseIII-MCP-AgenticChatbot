# Note: This template references an existing Secret that must be created manually
# The Secret should be created before deploying this Helm chart
#
# To create the Secret:
# kubectl create secret generic habit-tracker-secrets \
#   --from-literal=DATABASE_URL="postgresql://..." \
#   --from-literal=JWT_SECRET="..." \
#   --from-literal=JWT_ALGORITHM="HS256" \
#   --from-literal=GEMINI_API_KEY="..."
#
# The Secret name is configured in values.yaml: secrets.existingSecret
